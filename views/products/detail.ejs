<%- include('../includes/head.ejs') %> 
<main class="container">
    <% if (errorMessage) { %>
        <div class="alert alert-warning" role="alert">
            <%= errorMessage[0].param + ': '+ errorMessage[0].msg %>
        </div>
        <% } %>
    <div class="container mt-12">
        <div class="row">
            <div class="col-md-12">
                <br>
                <h1> Detail Product</h1>
                <hr>
            </div>
            <div class="col-md-6">
                <img src="<%- img_path %>" alt="" width="400" height="400"/>
                
            </div>
            <div class="col-md-6">
                <h2><br> <%= product_name %><br></h2>
                <hr>
                <h3>Detail</h3>
                <h4><%- description %></h4>
                <hr>
                <h2>Price : <%= price %> ฿ </h2>
                <button type="submit" class="btn btn-warning"> <i class="fa fa-shopping-cart" aria-hidden="true"></i> Add to Cart</button>
            </div>
        </div>
    </div>
  </main>

<%- include('../includes/end.ejs') %>